<%= render 'shared/form_errors', target: @stage %>

<%= form_for(@stage) do |f| %>
  <div class="field">
    <%= f.label :name, "Názov" %><br />
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit "Uložiť" %>
  </div>
<% end %>
